if (place_meeting(x, y + 12,  obj_player) && running == false)
	{
	image_speed = 1;
	running = true;
	}
/*else if (place_meeting(x, y + 12,  obj_player) && running == true) {
	image_index = 13;
	running = false;
	}*/
